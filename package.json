{
  "name": "nkb-it-management-system",
  "version": "1.0.0",
  "description": "NKB IT Management System",
  "main": "server/src/server.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "migrate": "npm run migrate --prefix server",
    "seed": "npm run seed --prefix server",
    "seed:demo": "npm run seed:demo --prefix server",
    "test": "npm run test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
